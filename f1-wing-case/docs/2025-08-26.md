# Devlog – 2025-08-23 to 2025-08-26

### Restructuring
- Created a new case directory `f1-wing-case` to replace the earlier `pitzDailySteady-case`.
- Migrated core setup files (`blockMeshDict`, `snappyHexMeshDict`, `fvSchemes`, `fvSolution`, `controlDict`, etc.).
- Borrowed and adapted parts of the `motorbike` tutorial case for a more suitable setup.

### Geometry & Meshing
- Updated and cleaned geometry (`f1wing_single.stl`) and linked it properly in `snappyHexMeshDict`.
- Configured `surfaceFeaturesDict` for feature edge detection.
- Ran `snappyHexMesh -overwrite` successfully:
  - ~4.4M cells after 4 refinement levels.
  - Mesh quality passed with no fatal errors.
  - Boundary patches created: `inlet`, `outlet`, `sides`, `topBottom`, `wing`.

### Visualization
- Opened the case in ParaView using `paraFoam`.
- Confirmed boundary patches are visible and distinct.
- Verified mesh refinement around the wing using **Surface With Edges**.
- Exported first visualization screenshot for documentation.

### Next Steps
- Refine slice/clip visualizations to better show internal refinement around the wing.
- Prepare to run `checkMesh` and `simpleFoam` for first simulation results.
- Make `README.md` with workflow overview and link in images.
